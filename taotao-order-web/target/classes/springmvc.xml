<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:p="http://www.springframework.org/schema/p"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:dubbo="http://code.alibabatech.com/schema/dubbo"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
    http://www.springframework.org/schema/beans/spring-beans-4.2.xsd
    http://www.springframework.org/schema/mvc
    http://www.springframework.org/schema/mvc/spring-mvc-4.2.xsd
    http://code.alibabatech.com/schema/dubbo
    http://code.alibabatech.com/schema/dubbo/dubbo.xsd
    http://www.springframework.org/schema/context
    http://www.springframework.org/schema/context/spring-context-4.2.xsd">
    <!-- 加载配置文件 -->
    <context:property-placeholder location="classpath:tokenTime.properties" />
    <!-- 配置包扫描器，扫描所有需要带@Controller注解的类 -->
    <context:component-scan base-package="com.taotao.order.controller" />
    <!-- 配置注解驱动 -->
    <mvc:annotation-driven />
    <!-- 配置视图解析器 -->
    <bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
        <property name="prefix" value="/WEB-INF/jsp/" />
        <property name="suffix" value=".jsp" />
    </bean>
    <!--配置多媒体解析器-->
    <bean id="multipartResolver" class="org.springframework.web.multipart.commons.CommonsMultipartResolver">
    <!--设定默认编码-->
        <property name="defaultEncoding" value="UTF-8" />
        <property name="maxUploadSize" value="5242880" />
    </bean>

    <!-- 配置资源映射标签 -->
    <mvc:resources location="/js/" mapping="/js/**" />
    <mvc:resources location="/css/" mapping="/css/**" />
    <mvc:resources location="/images/" mapping="/images/**" />
    
    <!--配置订单系统的拦截器，需要用户登录才允许放行-->
    <mvc:interceptors>
        <mvc:interceptor>
            <!--/order/** 表示拦截当前路径及其子路径    order/*表示拦截当前路径-->
            <mvc:mapping path="/order/**"/>
            <bean class="com.taotao.order.controller.userLoginCeptor"></bean>
        </mvc:interceptor>
    </mvc:interceptors>

    <!-- 引用dubbo服务 ：需要先引入dubbo的约束   version="1.0-SNAPSHOT"      -->
    <dubbo:application name="taotao-order-web"/>
    <dubbo:registry protocol="zookeeper" address="39.108.185.153:2181" />
    <dubbo:reference interface="com.taotao.CartFotInter" id="cartFotInter" check="false" timeout="120000"/>
    <dubbo:reference interface="com.taotao.sso.UserRegister" id="userRegister" check="false"/>
    <dubbo:reference interface="com.taotao.services.TbitemList" id="tbitemList" check="false"/>
    <dubbo:reference interface="com.taotao.OrderInt" id="orderInt" check="false"/>
    <!--<dubbo:reference interface="com.taotao.services.ItemCatService" id="itemCatService" check="false"/>-->
    <!--<dubbo:reference interface="com.taotao.services.Tbitemadd" id="tbitemadd" check="false"/>-->
    <!--&lt;!&ndash;<dubbo:reference interface="com.taotao.services.PicUpload" id="picUpload" check="false"/>&ndash;&gt;-->
    <!--<dubbo:reference interface="com.taotao.content.services.TbcategoryList" id="tbcategoryList" check="false"/>-->
    <!--<dubbo:reference interface="com.taotao.content.services.TbContenList" id="tbContenList" check="false"/>-->
    <!--<dubbo:reference interface="com.taotao.search.inter.SearchItem" id="searchItem" check="false"/>-->


</beans>